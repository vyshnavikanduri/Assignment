//we calling the GCD function recursively base condition is b=0 then we should return a
class Solution {
    public static int gcd(int a, int b) {
        if(b==0)
            return a;
        else
            return gcd(b,a%b);
    }
}
